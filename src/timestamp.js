export class Timestamp {
        constructor(url, time, dateCreated, note) {
                this.url = url;
                this.time = time;
                this.dateCreated = dateCreated;
                this.note = note;
        }
}